import type { CapacitorConfig } from '@capacitor/cli'

/**
 * Обёртка, превращающая собранный сайт в устанавливаемое приложение.
 *
 * Файлы кладутся внутрь APK, поэтому приложение работает без интернета и без
 * ссылки, а данные живут в приватном хранилище приложения — очистка браузера
 * их больше не трогает.
 *
 * `appId` менять нельзя: Android считает приложения с разными идентификаторами
 * разными программами. Смена превратит обновление в установку второй копии
 * рядом, со своими, пустыми данными.
 */
const config: CapacitorConfig = {
  appId: 'io.github.am2910.wallet',
  appName: 'Кошелёк',
  webDir: 'dist-app',
  android: {
    // Фон подложки под веб-слоем: без него при запуске моргает белым.
    backgroundColor: '#F5F7FA',
  },
  ios: {
    backgroundColor: '#F5F7FA',
  },
}

export default config
